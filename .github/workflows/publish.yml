name: Publish

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version without leading v, for example 0.1.0"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: publish-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-release:
    name: Build and package release
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable

      - name: Install build dependencies
        run: |
          sudo apt-get update -q
          sudo apt-get install -y --no-install-recommends \
            libgtk-4-dev libadwaita-1-dev pkg-config \
            fuse libfuse2 git sqlite3

      - name: Show toolchain
        run: |
          rustc --version
          cargo --version

      - name: Build
        run: cargo build --workspace --release --locked

      - name: Derive version
        id: version
        env:
          VERSION_INPUT: ${{ inputs.version }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="$VERSION_INPUT"
          else
            VERSION="${GITHUB_REF_NAME#v}"
          fi
          if ! grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$' <<< "$VERSION"; then
            echo "version must look like MAJOR.MINOR.PATCH, got: $VERSION" >&2
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Create dist directory
        run: mkdir -p dist

      # Tarball
      - name: Package tarball
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          tar -czf "dist/archductor-${VERSION}-linux-x86_64.tar.gz" \
            -C target/release archductor archductor-gtk

      # .deb and .rpm via nfpm
      - name: Install nfpm
        run: |
          set -euxo pipefail
          NFPM_VERSION="2.38.0"
          curl -fsSL \
            "https://github.com/goreleaser/nfpm/releases/download/v${NFPM_VERSION}/nfpm_${NFPM_VERSION}_Linux_x86_64.tar.gz" \
            | tar -xz -C /usr/local/bin nfpm
          nfpm --version

      - name: Build .deb
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: nfpm package --packager deb --target dist/

      - name: Build .rpm
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: nfpm package --packager rpm --target dist/

      # AppImage
      - name: Install appimagetool
        run: |
          set -euxo pipefail
          curl -fsSL -o /usr/local/bin/appimagetool \
            "https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage"
          chmod +x /usr/local/bin/appimagetool

      - name: Assemble AppDir
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euxo pipefail
          APPDIR="packaging/appimage/archductor.AppDir"
          install -Dm755 target/release/archductor "$APPDIR/usr/bin/archductor"
          install -Dm755 target/release/archductor-gtk "$APPDIR/usr/bin/archductor-gtk"
          # SVG icon is committed at $APPDIR/archductor.svg

      - name: Build AppImage
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          APPDIR="packaging/appimage/archductor.AppDir"
          appimagetool --appimage-extract-and-run "$APPDIR" \
            "dist/archductor-${VERSION}-x86_64.AppImage"

      - name: Generate checksums
        run: |
          cd dist
          sha256sum ./* > SHA256SUMS

      - name: Scan release artifacts with Trivy
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: fs
          scan-ref: dist
          exit-code: "1"
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          scanners: vuln
          timeout: 10m

      - name: Generate release scan SARIF
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        if: always()
        with:
          scan-type: fs
          scan-ref: dist
          format: sarif
          output: trivy-release-artifacts.sarif
          exit-code: "0"
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          scanners: vuln
          timeout: 10m

      - name: Upload release scan SARIF artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: trivy-release-artifacts-sarif
          path: trivy-release-artifacts.sarif
          if-no-files-found: error

      # Upload
      - name: Upload all artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: archductor-${{ github.ref_name }}-packages
          path: dist/*

      - name: Attach artifacts to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          files: |
            dist/*.tar.gz
            dist/*.deb
            dist/*.rpm
            dist/*.AppImage
            dist/SHA256SUMS

  build-windows-release:
    name: Build Windows release
    runs-on: windows-2025
    timeout-minutes: 45
    permissions:
      contents: write
    env:
      CARGO_TARGET_X86_64_PC_WINDOWS_GNU_LINKER: C:\msys64\ucrt64\bin\gcc.exe
      CC_x86_64_pc_windows_gnu: C:\msys64\ucrt64\bin\gcc.exe
      AR_x86_64_pc_windows_gnu: C:\msys64\ucrt64\bin\ar.exe
      PKG_CONFIG: C:\msys64\ucrt64\bin\pkgconf.exe
      PKG_CONFIG_ALLOW_CROSS: "1"
      PKG_CONFIG_PATH: C:\msys64\ucrt64\lib\pkgconfig
      PKG_CONFIG_PATH_x86_64_pc_windows_gnu: C:\msys64\ucrt64\lib\pkgconfig
      PKG_CONFIG_x86_64_pc_windows_gnu: C:\msys64\ucrt64\bin\pkgconf.exe
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install Windows GTK toolchain
        uses: msys2/setup-msys2@66cd2cce69caa17b53920067426061ca1de3a884 # v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-pkgconf
            mingw-w64-ucrt-x86_64-gtk4
            mingw-w64-ucrt-x86_64-libadwaita

      - name: Add UCRT64 runtime to PATH
        shell: pwsh
        run: Add-Content -Path $env:GITHUB_PATH -Value 'C:\msys64\ucrt64\bin'

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          targets: x86_64-pc-windows-gnu

      - name: Derive version
        id: version
        shell: pwsh
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
          VERSION_INPUT: ${{ inputs.version }}
        run: |
          if ($env:EVENT_NAME -eq 'workflow_dispatch') {
            $version = $env:VERSION_INPUT
          } else {
            $version = $env:REF_NAME -replace '^v', ''
          }
          if ($version -notmatch '^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$') {
            throw "version must look like MAJOR.MINOR.PATCH, got: $version"
          }
          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Build Windows binaries
        run: cargo build --workspace --release --target x86_64-pc-windows-gnu --locked

      - name: Assemble portable Windows bundle
        shell: pwsh
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          $bundle = "dist\archductor-$env:VERSION-windows-x86_64"
          New-Item -ItemType Directory -Force -Path $bundle | Out-Null
          Copy-Item target\x86_64-pc-windows-gnu\release\archductor.exe $bundle
          Copy-Item target\x86_64-pc-windows-gnu\release\archductor-gtk.exe $bundle
          Copy-Item target\x86_64-pc-windows-gnu\release\archcar.exe $bundle
          Copy-Item C:\msys64\ucrt64\bin\*.dll $bundle
          New-Item -ItemType Directory -Force -Path "$bundle\lib" | Out-Null
          Copy-Item C:\msys64\ucrt64\lib\gdk-pixbuf-2.0 "$bundle\lib" -Recurse
          if (Test-Path C:\msys64\ucrt64\lib\gtk-4.0) {
            Copy-Item C:\msys64\ucrt64\lib\gtk-4.0 "$bundle\lib" -Recurse
          }
          New-Item -ItemType Directory -Force -Path "$bundle\share" | Out-Null
          Copy-Item C:\msys64\ucrt64\share\glib-2.0 "$bundle\share" -Recurse
          Copy-Item C:\msys64\ucrt64\share\icons "$bundle\share" -Recurse
          Copy-Item C:\msys64\ucrt64\share\mime "$bundle\share" -Recurse
          $originalPath = $env:PATH
          $env:PATH = (($env:PATH -split ';') | Where-Object {
            $_ -and ($_ -notlike 'C:\msys64\ucrt64\bin*')
          }) -join ';'
          try {
            Push-Location $bundle
            .\archductor.exe doctor
          } finally {
            Pop-Location
            $env:PATH = $originalPath
          }
          Compress-Archive -Path "$bundle\*" -DestinationPath "$bundle.zip"
          $archive = Get-Item "$bundle.zip"
          $hash = (Get-FileHash -Algorithm SHA256 $archive.FullName).Hash.ToLowerInvariant()
          "$hash  $($archive.Name)" | Set-Content dist\SHA256SUMS-windows.txt

      - name: Upload Windows artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: archductor-${{ github.ref_name }}-windows
          path: |
            dist/*.zip
            dist/SHA256SUMS-windows.txt
          if-no-files-found: error

      - name: Attach Windows artifact to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3.0.1
        with:
          files: |
            dist/*.zip
            dist/SHA256SUMS-windows.txt
